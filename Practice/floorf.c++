#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    // using floor function which return
    // floor of input value
    float x;
    cout << "\nEnter a number: ";
    cin >> x;
    cout << "\nCeil is : " << ceil(x) << endl;

    return 0;
}