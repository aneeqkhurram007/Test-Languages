#include <iostream>
using namespace std;
int main()
{

    int num, sum = 0;
    cout << "Enter a number : ";
    cin >> num;
    sum = sum + num;
    cout << "Enter a number : ";
    cin >> num;
    sum = sum + num;
    cout << "Enter a number : ";
    cin >> num;
    sum = sum + num;
    cout << "Enter a number : ";
    cin >> num;
    sum = sum + num;
    cout << "Enter a number : ";
    cin >> num;
    sum = sum + num;
    cout << "Sum is : " << sum;
}